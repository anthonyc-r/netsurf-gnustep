{
    "## Comment" = "Do NOT change this file, Gorm maintains it";
    BrowserWindowController = {
	Actions = (
	    "back:",
	    "forward:",
	    "navigate:",
	    "stopOrRefresh:"
	);
	Outlets = (
	    backButton,
	    forwardButton,
	    urlBar,
	    plotView,
	    scrollView,
	    refreshButton
	);
	Super = NSWindowController;
    };
    FirstResponder = {
	Actions = (
	    "back:",
	    "forward:",
	    "navigate:",
	    "stopOrRefresh:"
	);
	Super = NSObject;
    };
    PlotView = {
	Actions = (
	);
	Outlets = (
	);
	Super = NSView;
    };
}