{
    "## Comment" = "Do NOT change this file, Gorm maintains it";
    BrowserWindowController = {
	Actions = (
	    "back:",
	    "forward:",
	    "navigate:",
	    "stopOrRefresh:",
	    "enterUrl:"
	);
	Outlets = (
	    backButton,
	    forwardButton,
	    urlBar,
	    plotView,
	    scrollView,
	    refreshButton
	);
	Super = NSWindowController;
    };
    FirstResponder = {
	Actions = (
	    "back:",
	    "forward:",
	    "navigate:",
	    "enterUrl:",
	    "stopOrRefresh:"
	);
	Super = NSObject;
    };
    PlotView = {
	Actions = (
	);
	Outlets = (
	);
	Super = NSView;
    };
}