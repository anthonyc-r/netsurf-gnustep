{
    "## Comment" = "Do NOT change this file, Gorm maintains it";
    BrowserWindowController = {
	Actions = (
	    "back:",
	    "forward:",
	    "navigate:",
	    "stopOrRefresh:",
	    "enterUrl:",
	    "enterSearch:"
	);
	Outlets = (
	    backButton,
	    forwardButton,
	    urlBar,
	    plotView,
	    scrollView,
	    refreshButton,
	    searchImage,
	    searchBar
	);
	Super = NSWindowController;
    };
    FirstResponder = {
	Actions = (
	    "back:",
	    "enterUrl:",
	    "forward:",
	    "navigate:",
	    "enterSearch:",
	    "stopOrRefresh:"
	);
	Super = NSObject;
    };
    PlotView = {
	Actions = (
	);
	Outlets = (
	);
	Super = NSView;
    };
}